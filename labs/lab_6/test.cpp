#include <iostream>
#include <map>
using namespace std;

int main(int argc, char const *argv[]) {
    map<int, map<int, int> > mymap;
    for (int i = 10; i<20; i++){
        mymap.insert(pair<i, pair<i, i>)
    }
}
